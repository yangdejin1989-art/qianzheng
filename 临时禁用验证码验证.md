# 临时禁用前端验证码验证（仅用于测试）

## ⚠️ 警告

此方法仅用于测试诊断，生产环境不建议使用！

## 修改步骤

1. 打开文件：`qianzheng/client/src/ApplyForm.js`

2. 找到第252-255行：

```javascript
if (!captchaValid) {
  showError('❌ 验证码错误！\n请重新输入正确的验证码');
  return;
}
```

3. 临时注释掉这几行（在前面加上 `//`）：

```javascript
// if (!captchaValid) {
//   showError('❌ 验证码错误！\n请重新输入正确的验证码');
//   return;
// }
```

4. 保存文件

5. 刷新浏览器页面（Ctrl + F5 强制刷新）

6. 再次尝试提交申请

## 还原修改

测试完成后，记得把注释删除，恢复原样。

---

**注意**：如果修改后仍然失败，说明问题不在验证码，需要进一步诊断。

